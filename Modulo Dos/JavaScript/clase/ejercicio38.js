// contador de digitos
let sum  = 0;
let cantidaDigitos = Math.floor(Math.random() * ((1000+1) - 1) + 1);
let longuitud = String(cantidaDigitos)
let resultado = longuitud.length;
console.log(resultado);